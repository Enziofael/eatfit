<UserControl x:Class="EatfitDesktop.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Welcome to Eatfit!" FontSize="32" 
                       FontWeight="Bold" Foreground="#667eea"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding WelcomeMessage}" FontSize="16"
                       Foreground="#666" HorizontalAlignment="Center" Margin="0,10,0,0"/>

            <!-- Кнопка выхода -->
            <Button Content="Logout" 
                    Command="{Binding LogoutCommand}"
                    Style="{StaticResource PrimaryButton}"
                    Width="200" Margin="0,30,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>