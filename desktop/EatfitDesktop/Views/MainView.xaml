<UserControl x:Class="EatfitDesktop.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель навигации -->
        <Border Grid.Row="0" Background="#667eea">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button x:Name="FeedBtn" Content="📰 Feed" 
                        Style="{StaticResource NavButton}" Click="FeedBtn_Click"/>
                <Button x:Name="DiaryBtn" Content="📅 Diary" 
                        Style="{StaticResource NavButton}" Click="DiaryBtn_Click"/>
                <Button x:Name="MealsBtn" Content="🍽 Meals" 
                        Style="{StaticResource NavButton}" Click="MealsBtn_Click"/>
                <Button x:Name="MessagesBtn" Content="💬 Messages" 
                        Style="{StaticResource NavButton}" Click="MessagesBtn_Click"/>
                <Button x:Name="ProfileBtn" Content="👤 Profile" 
                        Style="{StaticResource NavButton}" Click="ProfileBtn_Click"/>
            </StackPanel>
        </Border>

        <!-- Контент -->
        <ContentControl Grid.Row="1" x:Name="ContentArea"/>
    </Grid>
</UserControl>